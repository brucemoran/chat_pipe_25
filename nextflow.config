
profiles {
    docker {
        // Tell Nextflow to use Docker for process execution
        process {
            executor = 'local'
            container = 'ubuntu:latest'  // A default image if one isn’t explicitly stated
            containerEngine = 'docker'
            runOptions = '--rm'          // Remove container after running
        }
    }
}